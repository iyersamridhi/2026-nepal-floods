name: Refresh bulletins

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Refresh MoFA + Twitter bulletins
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY || secrets.X_AI }}
          TWITTER_BEARER_TOKEN: ${{ secrets.TWITTER_BEARER_TOKEN }}
        run: |
          chmod +x scripts/refresh.sh
          ./scripts/refresh.sh

      - name: Commit updated data files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/bulletin.json data/twitter_bulletin.json data/scraped/
          if git diff --staged --quiet; then
            echo "No bulletin changes to commit"
            exit 0
          fi
          git commit -m "chore: refresh official and Twitter bulletins"
          # Rebase onto latest main in case another push landed during the run
          git pull --rebase origin main
          git push
